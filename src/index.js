import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

// Create root element in DOM
const root = ReactDOM.createRoot(document.getElementById('root'));

// Render the App under the root element
root.render(
  <App />
);
